spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        format_sql: true
#       show_sql: true
#ddl-auto 테이블 create 해준다.
#show_Sql은 system.out을 통해 보임 실제 업무에서는 주석처리

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
#하이버네이트가 생성하는 SQL이 전부 debug로 logger를 통해 보임